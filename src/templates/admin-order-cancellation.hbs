<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{{subject}} - Maltiti A. Enterprise Ltd</title>
    <style>
        /* Reset styles */
        body, table, td, a {
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }
        table, td {
            /* Outlook-specific CSS properties for table spacing */
            mso-table-lspace: 0pt;
            mso-table-rspace: 0pt;
        }
        img {
            -ms-interpolation-mode: bicubic;
            border: 0;
            height: auto;
            line-height: 100%;
            outline: none;
            text-decoration: none;
        }

        /* Base styles */
        body {
            margin: 0;
            padding: 0;
            width: 100% !important;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #f4f7f6;
            color: #333333;
        }

        /* Container styles */
        .email-container {
            max-width: 700px;
            margin: 0 auto;
            background-color: #ffffff;
        }

        /* Header styles */
        .header {
            background-color: #ffffff;
            padding: 35px 25px 15px;
            text-align: center;
            border-bottom: 3px solid #dc3545;
        }

        .logo {
            max-width: 180px;
            height: auto;
            margin-bottom: 10px;
        }

        /* Content styles */
        .content {
            padding: 40px 30px;
        }

        .heading {
            color: #dc3545;
            font-size: 28px;
            font-weight: 700;
            margin: 0 0 20px 0;
            line-height: 1.3;
            text-align: center;
        }

        .section {
            margin-bottom: 30px;
        }

        .section-title {
            color: #dc3545;
            font-size: 20px;
            font-weight: 600;
            margin: 0 0 15px 0;
            border-bottom: 2px solid #dc3545;
            padding-bottom: 5px;
        }

        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        .info-table th, .info-table td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #e0e0e0;
        }

        .info-table th {
            background-color: #f8faf9;
            font-weight: 600;
            color: #dc3545;
        }

        .cancellation-details {
            background-color: #fff5f5;
            border: 1px solid #fed7d7;
            border-radius: 6px;
            padding: 20px;
            margin: 20px 0;
        }

        .cancellation-title {
            color: #dc3545;
            font-size: 18px;
            font-weight: 600;
            margin: 0 0 10px 0;
        }

        .cancellation-text {
            color: #333333;
            margin: 5px 0;
            line-height: 1.5;
        }

        .refund-info {
            background-color: #f0f9ff;
            border: 1px solid #90cdf4;
            border-radius: 6px;
            padding: 15px;
            margin: 15px 0;
        }

        .refund-title {
            color: #2b6cb0;
            font-size: 16px;
            font-weight: 600;
            margin: 0 0 8px 0;
        }

        /* Button styles */
        .button-container {
            margin: 35px 0;
            text-align: center;
        }

        .cta-button {
            display: inline-block;
            padding: 16px 48px;
            background-color: #dc3545;
            color: #ffffff !important;
            text-decoration: none;
            font-size: 16px;
            font-weight: 600;
            border-radius: 6px;
            transition: background-color 0.3s ease;
            box-shadow: 0 4px 6px rgba(220, 53, 69, 0.2);
        }

        .cta-button:hover {
            background-color: #c82333;
        }

        /* Footer styles */
        .footer {
            background-color: #f8faf9;
            padding: 30px;
            text-align: center;
            border-top: 1px solid #e0e0e0;
        }

        .footer-text {
            color: #777777;
            font-size: 13px;
            line-height: 1.6;
            margin: 0 0 10px 0;
        }

        .footer-link {
            color: #dc3545;
            text-decoration: none;
            font-weight: 600;
        }

        .footer-link:hover {
            text-decoration: underline;
        }

        /* Responsive styles */
        @media screen and (max-width: 600px) {
            .email-container {
                width: 100% !important;
            }

            .header, .content, .footer {
                padding: 25px 20px !important;
            }

            .heading {
                font-size: 24px !important;
            }

            .logo {
                max-width: 150px !important;
            }

            .cta-button {
                padding: 14px 36px !important;
                font-size: 15px !important;
            }

            .section-title {
                font-size: 18px !important;
            }

            .info-table {
                font-size: 14px;
            }
        }

        /* High contrast mode support */
        @media (prefers-contrast: more) {
            .cta-button {
                border: 2px solid #ffffff;
            }
        }
    </style>
</head>
<body>
    <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
        <tr>
            <td style="padding: 20px 0;">
                <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%" class="email-container">
                    <!-- Header -->
                    <tr>
                        <td class="header">
                            <img src="https://res.cloudinary.com/deyszqsgf/image/upload/v1764454227/maltiti-logo_xusduy.png"
                                 alt="Maltiti A. Enterprise Ltd Logo"
                                 class="logo"
                                 width="100">
                        </td>
                    </tr>

                    <!-- Content -->
                    <tr>
                        <td class="content">
                            <h1 class="heading">{{subject}}</h1>

                            <div class="section">
                                <h2 class="section-title">Order Details</h2>
                                <table class="info-table">
                                    <tr>
                                        <th>Order ID</th>
                                        <td>{{orderId}}</td>
                                    </tr>
                                    <tr>
                                        <th>Cancelled By</th>
                                        <td>{{cancelledBy}}</td>
                                    </tr>
                                </table>
                            </div>

                            <div class="section">
                                <h2 class="section-title">Customer Information</h2>
                                <table class="info-table">
                                    <tr>
                                        <th>Name</th>
                                        <td>{{customerName}}</td>
                                    </tr>
                                    <tr>
                                        <th>Email</th>
                                        <td>{{customerEmail}}</td>
                                    </tr>
                                </table>
                            </div>

                            <div class="cancellation-details">
                                <h3 class="cancellation-title">Cancellation Details</h3>
                                {{#if cancellationReason}}
                                <p class="cancellation-text"><strong>Reason:</strong> {{cancellationReason}}</p>
                                {{/if}}
                                {{#if refundAmount}}
                                <p class="cancellation-text"><strong>Refund Amount:</strong> GHS {{refundAmount}}</p>
                                {{/if}}
                                {{#if penaltyAmount}}
                                <p class="cancellation-text"><strong>Penalty Amount:</strong> GHS {{penaltyAmount}}</p>
                                {{/if}}
                                {{#if refundProcessed}}
                                <p class="cancellation-text"><strong>Refund Status:</strong> Processed</p>
                                {{else}}
                                <p class="cancellation-text"><strong>Refund Status:</strong> Not applicable or pending</p>
                                {{/if}}
                            </div>

                            {{#if refundAmount}}
                            <div class="refund-info">
                                <h4 class="refund-title">Refund Information</h4>
                                <p class="cancellation-text">
                                    A refund of GHS {{refundAmount}} has been processed for this order.
                                    {{#if penaltyAmount}}
                                    This includes a GHS {{penaltyAmount}} cancellation penalty.
                                    {{/if}}
                                    The customer will receive the refund within 7-12 business days.
                                </p>
                            </div>
                            {{/if}}

                            <!-- CTA Button -->
                            <div class="button-container">
                                <a href="{{url}}"
                                   class="cta-button"
                                   target="_blank"
                                   role="button"
                                   aria-label="{{action}}">
                                    {{action}}
                                </a>
                            </div>
                        </td>
                    </tr>

                    <!-- Footer -->
                    <tr>
                        <td class="footer">
                            <p class="footer-text">
                                This is an automated notification for order cancellations.
                            </p>

                            <p class="footer-text">
                                Email sent by maltitiaenterprise.com <br>
                                &copy; 2026 Maltiti A. Enterprise Ltd. All rights reserved.
                            </p>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>
</body>
</html>
