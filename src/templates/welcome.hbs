<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{{subject}} - Maltiti A. Enterprise Ltd</title>
    <style>
        /* Reset styles */
        body, table, td, a {
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }
        table, td {
            /* Outlook-specific CSS properties for table spacing */
            mso-table-lspace: 0pt;
            mso-table-rspace: 0pt;
        }
        img {
            -ms-interpolation-mode: bicubic;
            border: 0;
            height: auto;
            line-height: 100%;
            outline: none;
            text-decoration: none;
        }

        /* Base styles */
        body {
            margin: 0;
            padding: 0;
            width: 100% !important;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #f4f7f6;
            color: #333333;
        }

        /* Container styles */
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }

        /* Header styles */
        .header {
            background-color: #ffffff;
            padding: 35px 25px 15px;
            text-align: center;
            border-bottom: 3px solid #0F6938;
        }

        .logo {
            max-width: 180px;
            height: auto;
            margin-bottom: 10px;
        }

        /* Content styles */
        .content {
            padding: 40px 30px;
            text-align: center;
        }

        .heading {
            color: #0F6938;
            font-size: 28px;
            font-weight: 700;
            margin: 0 0 20px 0;
            line-height: 1.3;
        }

        .greeting {
            color: #333333;
            font-size: 16px;
            line-height: 1.6;
            margin: 0 0 15px 0;
        }

        .message {
            color: #555555;
            font-size: 15px;
            line-height: 1.7;
            margin: 0 0 30px 0;
            text-align: left;
        }

        /* Button styles */
        .button-container {
            margin: 35px 0;
        }

        .cta-button {
            display: inline-block;
            padding: 16px 48px;
            background-color: #0F6938;
            color: #ffffff !important;
            text-decoration: none;
            font-size: 16px;
            font-weight: 600;
            border-radius: 6px;
            transition: background-color 0.3s ease;
            box-shadow: 0 4px 6px rgba(15, 105, 56, 0.2);
        }

        .cta-button:hover {
            background-color: #0a4d28;
        }

        /* Social buttons */
        .social-buttons {
            margin: 30px auto;
            font-size: 0;
            text-align: center;
        }

        .social-button {
            display: inline-block;
            width: 50px;
            height: 50px;
            line-height: 50px;
            margin: 0 10px;
            text-align: center;
            position: relative;
            overflow: hidden;
            opacity: .99;
            border-radius: 50%;
            box-shadow: 0 0 30px 0 rgba(0, 0, 0, 0.05);
            -webkit-transition: all 0.35s cubic-bezier(0.31, -0.105, 0.43, 1.59);
            transition: all 0.35s cubic-bezier(0.31, -0.105, 0.43, 1.59);
        }
        .social-button:before {
            content: '';
            background-color: #000;
            width: 120%;
            height: 120%;
            position: absolute;
            top: 90%;
            left: -110%;
            -webkit-transform: rotate(45deg);
            transform: rotate(45deg);
            -webkit-transition: all 0.35s cubic-bezier(0.31, -0.105, 0.43, 1.59);
            transition: all 0.35s cubic-bezier(0.31, -0.105, 0.43, 1.59);
        }
        .social-button img {
            width: 38px;
            height: 38px;
            vertical-align: middle;
            -webkit-transform: scale(0.8);
            transform: scale(0.8);
            -webkit-transition: all 0.35s cubic-bezier(0.31, -0.105, 0.43, 1.59);
            transition: all 0.35s cubic-bezier(0.31, -0.105, 0.43, 1.59);
        }
        /* Facebook */
        .social-button.facebook:before {
            background-color: #3B5998;
        }
        .social-button.facebook img {
            filter: brightness(0) invert(1);
        }

        /* Instagram */
        .social-button.instagram:before {
            background-color: #E4405F;
        }
        .social-button.instagram img {
            filter: brightness(0) invert(1);
        }

        /* LinkedIn */
        .social-button.linkedin:before {
            background-color: #0077B5;
        }
        .social-button.linkedin img {
            filter: brightness(0) invert(1);
        }

        .social-button:focus:before, .social-button:hover:before {
            top: -10%;
            left: -10%;
        }
        .social-button:focus img, .social-button:hover img {
            -webkit-transform: scale(1);
            transform: scale(1);
        }

        /* Footer styles */
        .footer {
            background-color: #f8faf9;
            padding: 30px;
            text-align: center;
            border-top: 1px solid #e0e0e0;
        }

        .footer-text {
            color: #777777;
            font-size: 13px;
            line-height: 1.6;
            margin: 0 0 10px 0;
        }

        .footer-link {
            color: #0F6938;
            text-decoration: none;
            font-weight: 600;
        }

        .footer-link:hover {
            text-decoration: underline;
        }

        /* Responsive styles */
        @media screen and (max-width: 600px) {
            .email-container {
                width: 100% !important;
            }

            .header, .content, .footer {
                padding: 25px 20px !important;
            }

            .heading {
                font-size: 24px !important;
            }

            .logo {
                max-width: 150px !important;
            }

            .cta-button {
                padding: 14px 36px !important;
                font-size: 15px !important;
            }

            .message, .greeting {
                font-size: 14px !important;
            }
        }

        /* High contrast mode support */
        @media (prefers-contrast: more) {
            .cta-button {
                border: 2px solid #ffffff;
            }
        }
    </style>
</head>
<body>
    <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%">
        <tr>
            <td style="padding: 20px 0;">
                <table role="presentation" cellspacing="0" cellpadding="0" border="0" width="100%" class="email-container">
                    <!-- Header -->
                    <tr>
                        <td class="header">
                            <img src="https://res.cloudinary.com/deyszqsgf/image/upload/v1764454227/maltiti-logo_xusduy.png"
                                 alt="Maltiti A. Enterprise Ltd Logo"
                                 class="logo"
                                 width="100">
                        </td>
                    </tr>

                    <!-- Content -->
                    <tr>
                        <td class="content">
                            <h1 class="heading">{{subject}}</h1>

                            <p class="greeting">Hello <strong>{{name}}</strong>,</p>

                            <p class="message">{{body}}</p>

                            <!-- CTA Button -->
                            <div class="button-container">
                                <a href="{{url}}"
                                   class="cta-button"
                                   target="_blank"
                                   role="button"
                                   aria-label="{{action}}">
                                    {{action}}
                                </a>
                            </div>

                            <!-- Social Buttons -->
                            <div class="social-buttons">
                                <a class="social-button facebook" href="#" aria-label="Follow us on Facebook">
                                    <img src="https://img.icons8.com/color/48/facebook-new.png" alt="Facebook"/>
                                </a>
                                <a class="social-button instagram" href="#" aria-label="Follow us on Instagram">
                                    <img src="https://img.icons8.com/color/48/instagram-new--v1.png" alt="Instagram"/>
                                </a>
                                <a class="social-button linkedin" href="#" aria-label="Connect with us on LinkedIn">
                                    <img width="48" height="48" src="https://img.icons8.com/color/48/linkedin.png" alt="LinkedIn"/>
                                </a>
                            </div>
                        </td>
                    </tr>

                    <!-- Footer -->
                    <tr>
                        <td class="footer">
                            <p class="footer-text">
                                Stay in touch with us!
                            </p>

                            <p class="footer-text">
                                Email sent by maltitiaenterprise.com <br>
                                &copy; 2026 Maltiti A. Enterprise Ltd. All rights reserved.
                            </p>
                        </td>
                    </tr>
                </table>
            </td>
        </tr>
    </table>
</body>
</html>
