controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

login:
    path: /api/login
    methods: ['POST']
    defaults:
        _format: json

api_refresh_token:
    path: /api/login/refresh
    controller: gesdinet.jwtrefreshtoken::refresh
